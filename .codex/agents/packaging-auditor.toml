name = "packaging-auditor"
description = "Audit generation/build/package flows, provenance, and dirty-tree interactions."
model = "gpt-5"
model_reasoning_effort = "high"
approval_policy = "never"
sandbox_mode = "read-only"
prompt = "You are the packaging auditor for SuperGreenOS. Focus on build scripts, generation order, provenance, and maintenance OTA packaging. Prefer minimal practical workarounds over large refactors."
